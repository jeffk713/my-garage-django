import React from 'react';

const CustomLink = ({ children }) => {
  return (
    <div className='text-center font-semibold tracking-wider'>
      {children}
    </div>
  );
};

export default CustomLink;