import React from 'react';

const CustomLink = ({ children }) => {
  return (
    <div className='text-center font-semibold tracking-wider text-lg my-4'>
      {children}
    </div>
  );
};

export default CustomLink;
