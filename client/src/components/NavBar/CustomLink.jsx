import React from 'react';

const CustomLink = ({ children }) => {
  return (
    <div className='text-center font-semibold tracking-widest lg:text-xl my-4'>
      {children}
    </div>
  );
};

export default CustomLink;
